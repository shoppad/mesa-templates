{
  "setup": {
    "mode": "custom",
    "fields": [
        {
            "key": "webhook_url",
            "target": "custom.webhook_url",
            "label": "Install the Receive workflow on the other store and enter the webhook url",
            "description": "When you install the 'Receive Orders From Another Store' workflow in your other store, the Webhook trigger that thte workflow starts with will have a url that you can copy in here.",
            "tokens": false
        }
    ]
  }
}
